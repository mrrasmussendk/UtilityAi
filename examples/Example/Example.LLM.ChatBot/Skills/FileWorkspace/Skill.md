# file_workspace

Create simple folders and files in the current working directory.

Use shell commands:

- `mkdir -p notes`
- `touch notes/todo.txt`
- `mkdir -p output/reports`
- `printf "created by skill\n" > output/reports/readme.txt`
