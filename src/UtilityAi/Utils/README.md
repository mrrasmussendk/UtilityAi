# Utils

`Utils` contains foundational runtime primitives shared across the framework:

- `EventBus` for fact publication, retrieval, and scoped state
- `Runtime` passed through sensor/consideration/action execution
- Event bus snapshot and extension helpers for diagnostics and integration
