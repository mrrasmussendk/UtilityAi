# Orchestration

`Orchestration` coordinates the Utility AI loop (`Sense → Propose → Score → Act`).

This folder includes:
- `UtilityAiOrchestrator` and `IOrchestrator`
- Selection strategies and orchestration events
- Introspection and sink abstractions for observability
